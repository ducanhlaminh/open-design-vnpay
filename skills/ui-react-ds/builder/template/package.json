{
  "name": "uireact-ds-app",
  "private": true,
  "type": "module",
  "description": "Per-project UI-Spec (React DS) app. Screens compose from the imported design system's react bundle (staged by the daemon at src/ds/ — plain React + CSS-variable tokens, NO Tailwind). node_modules is the shared read-only uireact-base toolkit, so these versions are documentation — the actual resolved deps come from the image.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.1"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0"
  }
}
