// SAMPLE screen — replace with the real screens from the UX Spec. Screens
// compose from the design-system components staged at `src/ds/components/ui/`
// (import paths like `../ds/components/ui/button`) and style EXCLUSIVELY with
// the `tk-*` classes declared in `src/ds/styles/globals.css`.
export default function Home() {
  return (
    <div className="tk-bg-background" style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <p>Sample screen — the ui-react-ds skill replaces this with spec screens.</p>
    </div>
  )
}
